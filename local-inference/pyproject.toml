[project]
name = "omnimesh-local-inference"
version = "0.1.0"
description = "Placeholder local inference service (echo stub; swap for vllm/llama.cpp later)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "pydantic>=2.10.0",
    "uvicorn[standard]>=0.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
